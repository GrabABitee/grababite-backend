# # Database Configuration for Supabase PostgreSQL (using Session Pooler)
#     # Using the session pooler host, port, and username as explicitly shown in your Supabase screenshot.
#     spring.datasource.url=jdbc:postgresql://aws-0-ap-south-1.pooler.supabase.com:5432/postgres?sslmode=require
#     spring.datasource.username=postgres.pxyihmjoxnvyjsuirkop
#     spring.datasource.password=Eev6MYVirCRznYZN
#     spring.datasource.driver-class-name=org.postgresql.Driver

#     # JPA/Hibernate Configuration (keep as is)
#     spring.jpa.hibernate.ddl-auto=update
#     spring.jpa.show-sql=true
#     spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#     spring.jpa.properties.hibernate.format_sql=true

#     # UUID Generation Strategy (keep as is)
#     spring.jpa.properties.hibernate.id.new_generator_mappings=true
#     spring.jpa.properties.hibernate.id.uuid.uuid-generator-type=org.hibernate.id.uuid.UuidGenerator
# ===============================
# Database Configuration (Supabase Transaction Pooler)
# ===============================
spring.datasource.url=jdbc:postgresql://aws-0-ap-south-1.pooler.supabase.com:6543/postgres?sslmode=require
spring.datasource.username=postgres.pxyihmjoxnvyjsuirkop
spring.datasource.password=sMxWzNKLa2QlzkGr
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# HikariCP Connection Pool Settings
# ===============================
# Small pool is enough; transaction pooler multiplexes connections
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# ===============================
# JPA / Hibernate
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Dialect (Hibernate auto-detects, but keeping explicit is fine)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# UUID Generation Strategy
spring.jpa.properties.hibernate.id.new_generator_mappings=true
spring.jpa.properties.hibernate.id.uuid.uuid-generator-type=org.hibernate.id.uuid.UuidGenerator
